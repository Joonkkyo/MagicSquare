"""Boundary output — FR-OUT-* (U-OUT track)."""


def format_result(success: bool, detail: str = "") -> str:
    """U-OUT-01/02 — 성공/실패 표현 (미구현)."""
    raise NotImplementedError("GREEN: boundary.output.format_result")
