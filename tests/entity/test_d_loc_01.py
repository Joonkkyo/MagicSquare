"""D-LOC-01 — FR-LOC-01: find_blank_coords (1-index, row-major). RED skeleton."""

import pytest


def test_d_loc_01_blank_coords_row_major(grid_g1):
    # Given: G1 격자 (0이 2개) — grid_g1
    # When: find_blank_coords(grid_g1)  # RED: 호출은 GREEN/tdd-red에서
    # Then: [(2, 3), (4, 4)] — 1-index, row-major
    pytest.fail("RED: D-LOC-01 — 구현 없음, 의도적 실패")
